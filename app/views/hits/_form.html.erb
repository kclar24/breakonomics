<%= form_for(hit) do |f| %>
  <% if hit.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(hit.errors.count, "error") %> prohibited this hit from being saved:</h2>

      <ul>
      <% hit.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :image_url %>
    <%= f.text_field :image_url %>
  </div>

  <div class="field">
    <%= f.label :break_date %>
    <%= f.date_select :break_date %>
  </div>

  <div class="field">
    <%= f.label :description %>
    <%= f.text_area :description %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
